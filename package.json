{
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "workerBackend",
    "notification",
    "shared",
    "jobServer",
    "autoScaler"
  ],
  "allowScripts": {
    "cpu-features@0.0.10": true
  },
  "dependencies": {
    "@istream/shared": "^1.0.0"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test:unit --workspaces --if-present"
  }
}
